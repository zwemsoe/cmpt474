import { GraphQLProviderConfig } from '@aws-amplify/core/internals/utils';
import { EnumTypes } from '@aws-amplify/data-schema-types';
export declare const generateEnumsProperty: <T extends Record<any, any> = never>(graphqlConfig: GraphQLProviderConfig['GraphQL']) => EnumTypes<T>;

import { AmplifyServer } from '@aws-amplify/core/internals/adapter-core';
import { ClientWithModels, ListArgs, QueryArgs } from '../../types';
import { ModelIntrospectionSchema, SchemaModel } from '@aws-amplify/core/internals/utils';
export type IndexMeta = {
    queryField: string;
    pk: string;
    sk?: string[];
};
export declare function indexQueryFactory(client: ClientWithModels, modelIntrospection: ModelIntrospectionSchema, model: SchemaModel, indexMeta: IndexMeta, context?: boolean): ((contextSpec: AmplifyServer.ContextSpec, args: QueryArgs, options?: ListArgs) => Promise<any>) | ((args: QueryArgs, options?: ListArgs) => Promise<any>);
